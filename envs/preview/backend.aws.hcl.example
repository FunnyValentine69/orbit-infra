# Reference shape only. Generate the required backend.aws.hcl with
# scripts/write-preview-backend.sh; there is no example-file fallback.
# The per-env `key` is injected separately by the Makefile.
bucket       = "orbit-infra-79s5rw-tfstate"
region       = "us-east-1"
use_lockfile = true
encrypt      = true
