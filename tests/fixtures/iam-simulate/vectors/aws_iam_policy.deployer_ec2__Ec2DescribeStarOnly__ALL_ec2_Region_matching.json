{
  "schema_version": 1,
  "case_id": "case:aws_iam_policy.deployer_ec2:Ec2DescribeStarOnly:ALL:ec2:Region:matching",
  "document": "aws_iam_policy.deployer_ec2",
  "sid": "Ec2DescribeStarOnly",
  "simulation_mode": "custom",
  "assertion_kind": "decision",
  "action_names": [
    "ec2:DescribeAvailabilityZones",
    "ec2:DescribeInternetGateways",
    "ec2:DescribeNetworkInterfaces",
    "ec2:DescribeRegions",
    "ec2:DescribeRouteTables",
    "ec2:DescribeSecurityGroupRules",
    "ec2:DescribeSecurityGroups",
    "ec2:DescribeSubnets",
    "ec2:DescribeTags",
    "ec2:DescribeVpcAttribute",
    "ec2:DescribeVpcEndpoints",
    "ec2:DescribeVpcs"
  ],
  "resource_arns": [
    "*"
  ],
  "context_entries": [
    {
      "ContextKeyName": "ec2:Region",
      "ContextKeyValues": [
        "us-east-1"
      ],
      "ContextKeyType": "string"
    }
  ],
  "expect": {
    "decision": "allowed",
    "matched_sid_required": [
      "Ec2DescribeStarOnly"
    ],
    "matched_sid_forbidden": []
  },
  "notes": "aws iam simulate-principal-policy --policy-source-arn arn:aws:iam::${ACCOUNT_ID}:role/orbit-infra-${SUFFIX}-deployer --action-names <each row Action value> --resource-arns matching row resources; --context-entries ec2:Region:matching with every other condition key satisfying; expect allowed"
}
