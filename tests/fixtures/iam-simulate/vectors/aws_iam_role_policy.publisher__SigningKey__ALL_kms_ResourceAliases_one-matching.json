{
  "schema_version": 1,
  "case_id": "case:aws_iam_role_policy.publisher:SigningKey:ALL:kms:ResourceAliases:one-matching",
  "document": "aws_iam_role_policy.publisher",
  "sid": "SigningKey",
  "simulation_mode": "custom",
  "assertion_kind": "decision",
  "action_names": [
    "kms:DescribeKey",
    "kms:GetPublicKey",
    "kms:Sign",
    "kms:Verify"
  ],
  "resource_arns": [
    "arn:aws:kms:us-east-1:${ACCOUNT_ID}:key/preview"
  ],
  "context_entries": [
    {
      "ContextKeyName": "kms:ResourceAliases",
      "ContextKeyValues": [
        "alias/nonmatching-one",
        "alias/orbit-infra-${SUFFIX}-signing",
        "alias/nonmatching-two"
      ],
      "ContextKeyType": "stringList"
    }
  ],
  "expect": {
    "decision": "allowed",
    "matched_sid_required": [
      "SigningKey"
    ],
    "matched_sid_forbidden": []
  },
  "notes": "aws iam simulate-principal-policy --policy-source-arn arn:aws:iam::${ACCOUNT_ID}:role/orbit-infra-${SUFFIX}-publisher --action-names <each row Action value> --resource-arns matching row resources; --context-entries ContextKeyName=kms:ResourceAliases,ContextKeyValues=alias/nonmatching-one,alias/orbit-infra-${SUFFIX}-signing,alias/nonmatching-two,ContextKeyType=stringList; expect allowed"
}
