{
  "schema_version": 1,
  "case_id": "case:aws_iam_policy.deployer_elb_ecs:EcsListServicesClusterScoped:ALL:ecs:cluster:matching",
  "document": "aws_iam_policy.deployer_elb_ecs",
  "sid": "EcsListServicesClusterScoped",
  "simulation_mode": "custom",
  "assertion_kind": "decision",
  "action_names": [
    "ecs:ListServices"
  ],
  "resource_arns": [
    "*"
  ],
  "context_entries": [
    {
      "ContextKeyName": "ecs:cluster",
      "ContextKeyValues": [
        "arn:aws:ecs:us-east-1:${ACCOUNT_ID}:cluster/orbit-infra-${SUFFIX}-preview"
      ],
      "ContextKeyType": "string"
    }
  ],
  "expect": {
    "decision": "allowed",
    "matched_sid_required": [
      "EcsListServicesClusterScoped"
    ],
    "matched_sid_forbidden": []
  },
  "notes": "aws iam simulate-principal-policy --policy-source-arn arn:aws:iam::${ACCOUNT_ID}:role/orbit-infra-${SUFFIX}-deployer --action-names <each row Action value> --resource-arns matching row resources; --context-entries ecs:cluster:matching with every other condition key satisfying; expect allowed"
}
