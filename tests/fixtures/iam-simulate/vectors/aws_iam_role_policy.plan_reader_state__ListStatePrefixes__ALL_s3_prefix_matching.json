{
  "schema_version": 1,
  "case_id": "case:aws_iam_role_policy.plan_reader_state:ListStatePrefixes:ALL:s3:prefix:matching",
  "document": "aws_iam_role_policy.plan_reader_state",
  "sid": "ListStatePrefixes",
  "simulation_mode": "custom",
  "assertion_kind": "decision",
  "action_names": [
    "s3:ListBucket"
  ],
  "resource_arns": [
    "arn:aws:s3:::orbit-infra-${SUFFIX}-tfstate"
  ],
  "context_entries": [
    {
      "ContextKeyName": "s3:prefix",
      "ContextKeyValues": [
        "bootstrap/example"
      ],
      "ContextKeyType": "string"
    }
  ],
  "expect": {
    "decision": "allowed",
    "matched_sid_required": [
      "ListStatePrefixes"
    ],
    "matched_sid_forbidden": []
  },
  "notes": "aws iam simulate-principal-policy --policy-source-arn arn:aws:iam::${ACCOUNT_ID}:role/orbit-infra-${SUFFIX}-plan-reader --action-names <each row Action value> --resource-arns matching row resources; --context-entries s3:prefix:matching with every other condition key satisfying; expect allowed"
}
