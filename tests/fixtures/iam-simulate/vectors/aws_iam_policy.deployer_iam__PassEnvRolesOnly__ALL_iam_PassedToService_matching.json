{
  "schema_version": 1,
  "case_id": "case:aws_iam_policy.deployer_iam:PassEnvRolesOnly:ALL:iam:PassedToService:matching",
  "document": "aws_iam_policy.deployer_iam",
  "sid": "PassEnvRolesOnly",
  "simulation_mode": "custom",
  "assertion_kind": "decision",
  "action_names": [
    "iam:PassRole"
  ],
  "resource_arns": [
    "arn:aws:iam::${ACCOUNT_ID}:role/previeworbit-infra-${SUFFIX}preview"
  ],
  "context_entries": [
    {
      "ContextKeyName": "iam:PassedToService",
      "ContextKeyValues": [
        "ecs-tasks.amazonaws.com"
      ],
      "ContextKeyType": "string"
    }
  ],
  "expect": {
    "decision": "allowed",
    "matched_sid_required": [
      "PassEnvRolesOnly"
    ],
    "matched_sid_forbidden": []
  },
  "notes": "aws iam simulate-principal-policy --policy-source-arn arn:aws:iam::${ACCOUNT_ID}:role/orbit-infra-${SUFFIX}-deployer --action-names <each row Action value> --resource-arns matching row resources; --context-entries iam:PassedToService:matching with every other condition key satisfying; expect allowed"
}
