{
  "schema_version": 1,
  "case_id": "case:aws_iam_policy.deployer_iam:IamServiceLinkedRoleEcs:ALL:resource:nonmatching",
  "document": "aws_iam_policy.deployer_iam",
  "sid": "IamServiceLinkedRoleEcs",
  "simulation_mode": "custom",
  "assertion_kind": "decision",
  "action_names": [
    "iam:CreateServiceLinkedRole"
  ],
  "resource_arns": [
    "arn:aws:iam::${ACCOUNT_ID}:role/outside-${SUFFIX}-example"
  ],
  "context_entries": [
    {
      "ContextKeyName": "iam:AWSServiceName",
      "ContextKeyValues": [
        "ecs.amazonaws.com"
      ],
      "ContextKeyType": "string"
    }
  ],
  "expect": {
    "decision": "implicitDeny",
    "matched_sid_required": [],
    "matched_sid_forbidden": [
      "IamServiceLinkedRoleEcs"
    ]
  },
  "notes": "aws iam simulate-principal-policy --policy-source-arn arn:aws:iam::${ACCOUNT_ID}:role/orbit-infra-${SUFFIX}-deployer --action-names <each row Action value> --resource-arns same-type resource outside every row Resource pattern; --context-entries <every condition key satisfying>; expect implicitDeny"
}
