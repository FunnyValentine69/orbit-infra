{
  "schema_version": 1,
  "case_id": "case:aws_iam_policy.deployer_iam:DenyRoleMutationWrongBoundary:ALL:iam:PermissionsBoundary:absent",
  "document": "aws_iam_policy.deployer_iam",
  "sid": "DenyRoleMutationWrongBoundary",
  "simulation_mode": "custom-isolated",
  "assertion_kind": "decision",
  "action_names": [
    "iam:AttachRolePolicy",
    "iam:CreateRole",
    "iam:PutRolePolicy"
  ],
  "resource_arns": [
    "*"
  ],
  "context_entries": [],
  "expect": {
    "decision": "explicitDeny",
    "matched_sid_required": [
      "DenyRoleMutationWrongBoundary"
    ],
    "matched_sid_forbidden": []
  },
  "notes": "aws iam simulate-principal-policy --policy-source-arn arn:aws:iam::${ACCOUNT_ID}:role/orbit-infra-${SUFFIX}-deployer --action-names <each row Action value> --resource-arns matching row resources; --context-entries omitted (no entry for iam:PermissionsBoundary); expect explicitDeny attributed to this Sid via MatchedStatements (DenyRoleMutationMissingBoundary also matches an absent key), or an isolated simulate-custom-policy containing only the DenyRoleMutationWrongBoundary statement"
}
