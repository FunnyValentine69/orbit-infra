{
  "schema_version": 1,
  "case_id": "case:aws_iam_role_policy.publisher:SigningKey:ALL:resource:nonmatching",
  "document": "aws_iam_role_policy.publisher",
  "sid": "SigningKey",
  "simulation_mode": "custom",
  "assertion_kind": "decision",
  "action_names": [
    "kms:DescribeKey",
    "kms:GetPublicKey",
    "kms:Sign",
    "kms:Verify"
  ],
  "resource_arns": [
    "arn:aws:kms:us-west-2:${ACCOUNT_ID}:key/outside-key-id"
  ],
  "context_entries": [
    {
      "ContextKeyName": "kms:ResourceAliases",
      "ContextKeyValues": [
        "alias/orbit-infra-${SUFFIX}-signing"
      ],
      "ContextKeyType": "stringList"
    }
  ],
  "expect": {
    "decision": "implicitDeny",
    "matched_sid_required": [],
    "matched_sid_forbidden": [
      "SigningKey"
    ]
  },
  "notes": "aws iam simulate-principal-policy --policy-source-arn arn:aws:iam::${ACCOUNT_ID}:role/orbit-infra-${SUFFIX}-publisher --action-names <each row Action value> --resource-arns same-type resource outside every row Resource pattern; --context-entries <every condition key satisfying>; expect implicitDeny"
}
