{
  "schema_version": 1,
  "case_id": "case:aws_iam_policy.deployer_data:SigningPublicKeyRead:ALL:kms:ResourceAliases:absent",
  "document": "aws_iam_policy.deployer_data",
  "sid": "SigningPublicKeyRead",
  "simulation_mode": "custom",
  "assertion_kind": "decision",
  "action_names": [
    "kms:GetPublicKey"
  ],
  "resource_arns": [
    "arn:aws:kms:us-east-1:${ACCOUNT_ID}:key/preview"
  ],
  "context_entries": [],
  "expect": {
    "decision": "implicitDeny",
    "matched_sid_required": [],
    "matched_sid_forbidden": [
      "SigningPublicKeyRead"
    ]
  },
  "notes": "aws iam simulate-principal-policy --policy-source-arn arn:aws:iam::${ACCOUNT_ID}:role/orbit-infra-${SUFFIX}-deployer --action-names <each row Action value> --resource-arns matching row resources; no --context-entries for kms:ResourceAliases; expect implicitDeny"
}
